services:
  - type: web
    name: lotto-ai
    runtime: python
    buildCommand: chmod +x build.sh && ./build.sh
    startCommand: gunicorn --bind 0.0.0.0:$PORT lotto_server:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.8
      - key: PORT
        value: 5000
